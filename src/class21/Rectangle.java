package class21;

public class Rectangle {
    //Create 1 class in which create a methods that will calculate the area of
    //Rectangle
    //Square

    String calculateArea(int l, int w) {
        return "the area of Rectangle is " + (l * w);
    }

    String calculateArea(int l) {
        return "the area of Square is " + (l * l);
    }
}
